# Chin-Te Tsai — Academic Homepage

A responsive one-page academic website ready for GitHub Pages.

## Sections

- Full-screen hero
- Profile image
- About
- Research
- Contact

## Replace the images

1. Replace `assets/profile-placeholder.svg` with your portrait.
2. Replace `assets/hero-background.svg` with your preferred background image.
3. Keep the same filenames, or update the file paths in `index.html` and `styles.css`.

Recommended sizes:

- Portrait: square image, at least 800 × 800 px
- Hero background: landscape image, at least 1920 × 1080 px

## Preview locally

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

## Publish on GitHub Pages

1. Create a repository named `awsd1257.github.io`.
2. Upload all files in this folder to the repository root.
3. Open **Settings → Pages**.
4. Under **Build and deployment**, select **Deploy from a branch**.
5. Select the `main` branch and `/ (root)`.
6. Save.

Your website will be available at:

`https://awsd1257.github.io`

## Edit text

- Main content: `index.html`
- Colors and layout: `styles.css`
- Mobile navigation: `script.js`
